import { LegacyChartBridge } from "../components/chart/LegacyChartBridge";

/** React ownership boundary for the incrementally migrated chart page. */
export function DentalChartPage() {
  return <LegacyChartBridge />;
}
